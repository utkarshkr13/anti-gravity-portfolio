/* ============================================================
   MAGNETIC BUTTON EFFECT — DISABLED for stable button positions
   (UI revamp: buttons stay fixed; hover handled by CSS only)
   ============================================================ */

(function () {
  'use strict';

})();
